id(N, -1);
  // vb found(N, false);
  // function<void(ll)> dfs = [&](ll i) {
  //   if (found[i]) return;
  //   tour.back().pb(i);
  //   dfs(T[i]);
  // };
  // rep(i, N) {
  //   if (found[i]) continue;
  //   tour.pb({});
  //   dfs(i);
  //   reverse(all(tour.back()));
  // }