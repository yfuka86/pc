XT) {
  //   auto [l, r] = f(x, A[0]);

  //   bool zero = true;
  //   rep2(i, 1, N) {
  //     bool valid = false;
  //     rep2(y, 1, MAXT) {
  //       auto [tl, tr] = f(y, A[i]);
  //       if (!(tr < l || r < tl)) { valid = true; break; }
  //     }
  //     if (!valid) { zero = false; break; }
  //   }
  //   if (zero) { cout << 0 << "\n"; return; }
  // }