
  // vb visited(9, 0);

  // function<void(ll)> dfs = [&](ll v, vl hist) -> void {
  //   if (visited[v]) return;
  //   visited[v] = 1;
  //   hist.pb(v);
  //   for (ll next: G[v]) {
  //     dfs(next);
  //   }
  //   hist.pop_back();
  // };

  // rep(i, N)

  // coutarray(ans