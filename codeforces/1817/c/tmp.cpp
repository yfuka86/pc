
  // VEC(mint, a, d + 1);
  // VEC(mint, b, d + 1);

  // mint xda, xdb, xdseca, xdsecb;
  // rep(i, d + 1) {
  //   mint deno = fac(i) * fac(d - i);
  //   xda += a[i] / deno;
  //   xdb += b[i] / deno;
  // }